# Live compatibility results

Scheduled credential-gated workflows write one JSON result per provider here as an artifact. The documentation treats a missing result as “not live tested”; it never invents a passing status.
